"""Reusable face-match provenance pipeline.

The package contains no terminal, HTTP-framework, or browser interface code.
CLI and web-service adapters live at the repository root and import these
shared primitives.
"""

__version__ = "0.1.0"
